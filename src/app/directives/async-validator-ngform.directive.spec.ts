import { AsyncValidatorNgformDirective } from './async-validator-ngform.directive';

describe('AsyncValidatorNgformDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncValidatorNgformDirective();
    expect(directive).toBeTruthy();
  });
});
